/**
 * The model of a grid result.
 * The fields of the model are changed dynamically by
 * the Grid controller.
 */
Ext.define('OgamDesktop.model.result.Grid',{
	extend: 'Ext.data.Model'
	// Validation tasks to add ?
});