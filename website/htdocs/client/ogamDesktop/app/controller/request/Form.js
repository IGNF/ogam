Ext.define('OgamDesktop.controller.request.Form', {
	extend: 'Ext.app.Controller',
	
	/**
	 * Removes filters.
	 * @param field
	 */
	removeFilter: function(selectedFilter) {
		
	},
	
	/**
	 * Removes all filters.
	 * @param object
	 */
	removeAllFilter: function() {
		
	},
	
	/**
	 * Launches the request.
	 * @param columnFields
	 * @param filterFields
	 * @param filterFieldsValues
	 * @param objects
	 */
	launchRequest: function(columnFields, filterFields, filterFieldsValues, objects) {
		
		return results;
	}
});