Ext.define('Ogam.controller.request.AdvancedRequest',{
	extend: 'Ext.app.Controller',
	
	/**
	 * Adds a column for displaying in the result.
	 * @param field
	 */
	addColumn: function(selectedColumn) {
		
	},
	
	/**
	 * Removes columns.
	 * @param field
	 */
	removeColumn: function(selectedColumn) {
		
	},
	
	/**
	 * Adds all columns of an object.
	 * @param object
	 */
	addAllColumns: function(selectedObject) {
		
	},
	
	/**
	 * Removes all columns.
	 * @param object
	 */
	removeAllColumns: function() {
		
	},
	/**
	 * Adds a filter into the FormPanel.
	 * @param field
	 */
	addFilter: function(selectedFilter) {
		
	},
	
	/**
	 * Adds all filters of an object.
	 * @param object
	 */
	addAllFilters: function(selectedObject) {
		
	}
});